# Instalar el paquete del kernel
install.packages("IRkernel")
# Registrar el kernel para que aparezca en tus notebooks
IRkernel::installspec(user = FALSE, name = 'ir_wsl', displayname = 'R (MCDI-WSL)')
# Salir de R
q()
# Registrar el kernel solo para tu usuario actual
IRkernel::installspec(user = TRUE, name = 'ir_wsl', displayname = 'R (MCDI-WSL)')
q()
